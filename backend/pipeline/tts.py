# tts.py — Text-to-speech via Rime Arcana (Hindi)
#
# TODO (Phase 4): implement
#   synthesize(text: str) -> AsyncIterator[bytes]
# streaming Hindi audio chunks from Rime Arcana (modelId="arcana", lang="hin").
# The Hindi voice ID (picked in Phase 1) will live here as a module-level constant.
