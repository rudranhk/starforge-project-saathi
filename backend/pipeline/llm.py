# llm.py — Saathi's grounded response generation via Claude
#
# TODO (Phase 3): implement
#   generate(user_turn: str, policy_chunks: list[dict], history: list[dict]) -> str
# using claude-sonnet-5, composing the Saathi system prompt (see prompts.py)
# with conversation history and <policy_context> retrieved chunks.
