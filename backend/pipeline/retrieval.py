# retrieval.py — Policy chunk retrieval from Qdrant
#
# TODO (Phase 2): implement
#   retrieve(query: str, k: int = 5) -> list[dict]
# returning [{text, page_num, score}, ...] from the "saathi_policy" collection.
