# Only needed for a deployed build (Vercel) talking to a deployed backend.
# Local dev needs nothing here — app/page.tsx falls back to
# ws://localhost:8000/ws when this is unset.
NEXT_PUBLIC_WS_URL=wss://saathi-error404.onrender.com/ws
